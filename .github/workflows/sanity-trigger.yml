name: Sanity Trigger Rebuild

on:
  repository_dispatch:
    types: [sanity-update]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Acknowledge Rebuild
        run: echo "Sanity content change received. Hostinger build triggered."
